# =============================================================================
# requirements.txt — Reproducible Python Environment
# =============================================================================
# AI-Driven Network Traffic Anomaly Detection Pipeline
#
# Install:
#   pip install -r requirements.txt
#
# All versions are pinned for full reproducibility.
# Tested on Python 3.10 / 3.11 / 3.12.
# =============================================================================

# ---------------------------------------------------------------------------
# Core scientific stack
# ---------------------------------------------------------------------------
numpy==1.26.4           # Numerical arrays; underpins all ML libraries
pandas==2.2.2           # DataFrame manipulation for network flow CSVs
scipy==1.13.1           # Statistical utilities (used internally by sklearn)

# ---------------------------------------------------------------------------
# Machine Learning — Scikit-Learn
# ---------------------------------------------------------------------------
scikit-learn==1.5.1     # Random Forest, preprocessing, metrics, model selection

# ---------------------------------------------------------------------------
# Gradient Boosting
# ---------------------------------------------------------------------------
xgboost==2.0.3          # XGBoost classifier with GPU support

# ---------------------------------------------------------------------------
# Class Imbalance Handling
# ---------------------------------------------------------------------------
imbalanced-learn==0.12.3  # SMOTE implementation (wraps sklearn)

# ---------------------------------------------------------------------------
# Deep Learning — TensorFlow (CPU+GPU via CUDA auto-detection)
# ---------------------------------------------------------------------------
tensorflow==2.16.1      # Keras MLP; includes GPU support via CUDA 12.x
# If you prefer PyTorch instead, comment the line above and uncomment:
# torch==2.3.1
# torchvision==0.18.1

# ---------------------------------------------------------------------------
# Visualisation & Reporting
# ---------------------------------------------------------------------------
matplotlib==3.9.1       # ROC, PR, and confusion matrix plots
seaborn==0.13.2         # Enhanced statistical plots (optional, imported in evaluator)

# ---------------------------------------------------------------------------
# Utilities
# ---------------------------------------------------------------------------
tqdm==4.66.4            # Progress bars for long data-loading loops
joblib==1.4.2           # Model serialisation, parallel processing
PyYAML==6.0.1           # Reading YAML config files (cloud deployment)

# ---------------------------------------------------------------------------
# Cloud / AWS (optional — required only for SageMaker execution)
# ---------------------------------------------------------------------------
boto3==1.34.131         # AWS SDK — S3 data access, SageMaker API
sagemaker==2.224.4      # SageMaker Python SDK for managed training jobs
